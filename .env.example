# One key covers both models. https://vercel.com/ai-gateway
# Jev scores the menu (typesafe-ai/jev) and Gemini writes the observation (google/gemini-3.5-flash).
AI_GATEWAY_API_KEY=

# Photos come from KartaView, which needs no key at all.
